# Чеклист ментора

Проверка после дня 2. Смотри репозиторий студента и живой сервер, не «рассказ о том, что сделано».

## Окружение

- [ ] Есть `.env.example` с `PORT` и `DATABASE_URL`; самого `.env` в git нет
- [ ] `npm install` проходит
- [ ] `docker compose` поднимает Postgres 16 на 5432
- [ ] После `npm run db:migrate` и `npm run dev` сервер отвечает

## Готовый скелет (не ломали)

- [ ] `GET /health` → `{ "ok": true }`
- [ ] User CRUD на `/users`: 200 / 201 / 204 / 400 / 404 как в README
- [ ] `POST /users` без `email` или `name` → 400
- [ ] Нет JWT, сессий, middleware «авторизации»

## Практика студента (день 1)

- [ ] Файл `answers/day1.md`: 5–7 предложений своими словами
- [ ] В тексте есть факт про статус на несуществующий id (ожидаем 404)

## Практика студента (день 2)

- [ ] В `schema.prisma` модель Task: `title`, `done @default(false)`, `userId`, relation на User
- [ ] У User есть `tasks Task[]`
- [ ] Есть миграция после добавления Task
- [ ] Модуль `src/modules/tasks/` повторяет структуру users (routes + handlers)
- [ ] Маршруты зарегистрированы в `app.ts`
- [ ] `POST /tasks` без `title` или `userId` → 400
- [ ] `GET`/`PATCH`/`DELETE /tasks/:id` для отсутствующей записи → 404
- [ ] Успешный `POST /tasks` → 201, `DELETE` → 204
- [ ] Задача привязана к существующему user (`userId`)

## Качество (не архитектура ради архитектуры)

- [ ] Handlers читаемые, без лишних слоёв «service/repository/dto»
- [ ] Статусы не свалены в один 200
- [ ] Код студента похож на users, а не на сгенерированный каркас с другой стилистикой

## Опционально

- [ ] `GET /users/:id/tasks` работает и отдаёт 404, если пользователя нет

## Вердикт

- **Зачёт** — скелет жив, день 1 описан, Task CRUD руками, статусы сходятся.
- **На доработку** — нет миграции / нет 400 на пустой POST / Task не связан с User / ответы дня 1 сгенерированы без фактов с сервера.
